apply plugin: 'com.android.library'

android {
    compileSdkVersion 16
    buildToolsVersion "25.0.2"

    defaultConfig {

        minSdkVersion 16
        targetSdkVersion 24
        versionCode 1
        versionName version

    }

}
